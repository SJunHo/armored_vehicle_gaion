<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.gaion.armoredVehicle.web.analysis.mapper.DriverattitdMapper">
	<select id="findDriverattitdAll" resultType="kr.gaion.armoredVehicle.web.analysis.model.DriverAttitd">
		select *
		from DRIVERATTITD
	</select>
	<select id="findDriverattitdByDaid" parameterType="String" resultType="kr.gaion.armoredVehicle.web.analysis.model.DriverAttitd">
		select *
		from DRIVERATTITD
		where daid = #{daid}
		order by bizlogic
	</select>
	
	<update id="updateDriverAttitd" parameterType="kr.gaion.armoredVehicle.web.analysis.model.DriverAttitd">
		update DRIVERATTITD
		set STDVAL = #{stdval}, msg = #{msg},
			snsrid = #{snsrid},
			usedvcd = #{usedvcd},
			mdfr = #{mdfr}, mdfcdt = #{mdfcdt}
		where daid = #{daid} and bizlogic = #{bizlogic}
	</update>
</mapper>