<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.gaion.armoredVehicle.web.analysis.mapper.EngdataMapper">

	<insert id="insertEngdata" parameterType="kr.gaion.armoredVehicle.web.analysis.model.Engdata">
		insert into ENGDATA(SDAID ,OPERDATE ,OPERTIME ,DATE ,TIME ,AI_Predict 
		,W_RPM ,E_V_OverallRMS ,`E_V_1_2X` ,E_V_1X ,E_V_Crestfactor ,AC_h ,AC_v ,AC_a ,LA ,LO ,FILENM)
		values(#{sdaid}, #{operdate}, #{opertime}, #{date}, #{time}, #{AI_Predict}
		, #{W_RPM}, #{E_V_OverallRMS}, #{E_V_1_2X}, #{E_V_1X}, #{E_V_Crestfactor}
		, #{AC_h}, #{AC_v}, #{AC_a}, #{LA}, #{LO}, #{filenm})
	</insert>
	
	<insert id="insertEngsim" parameterType="kr.gaion.armoredVehicle.web.analysis.model.Engdata">
		insert into ENGSIM(SDAID, OPERDATE, OPERTIME, DATE, TIME, AI_Predict,
		 W_RPM, E_V_OverallRMS,`E_V_1_2X` , E_V_1X, E_V_Crestfactor, AC_h, AC_v, AC_a, LA, LO, FILENM)
		values(#{sdaid}, #{operdate}, #{opertime}, #{date}, #{time}, #{AI_Predict}, 
		#{W_RPM}, #{E_V_OverallRMS}, #{E_V_1_2X}, #{E_V_1X}, #{E_V_Crestfactor}, #{AC_h},
		#{AC_v}, #{AC_a}, #{LA}, #{LO}, #{filenm})
	</insert>
	
	<select id="findEngdataBySdaidAndFilenm" parameterType="java.util.Map" 
	resultType="kr.gaion.armoredVehicle.web.analysis.model.Engdata">
		select *
		from ENGDATA
		where SDAID = #{sdaid}
		and FILENM = #{filenm}
	</select>
	
	<select id="countEngdataByTable" parameterType="kr.gaion.armoredVehicle.web.analysis.model.troubleDataRequest" resultType="Integer">
		select ifnull(count(*),0)
		from ENGDATA
		where sdaid = #{sdaid} 
		and DATE between STR_TO_DATE(#{startDate}, '%Y-%m-%d %H:%i:%s') and STR_TO_DATE(#{endDate}, '%Y-%m-%d %H:%i:%s')
	</select>
	
	<select id="findEngdata" parameterType="kr.gaion.armoredVehicle.web.analysis.model.troubleDataRequest" resultType="kr.gaion.armoredVehicle.web.analysis.model.Engdata">
		select s.SDANM, e.*
		from ENGDATA e, SDA s
		where e.SDAID = s.SDAID
		and e.SDAID = #{sdaid} 
		and DATE between STR_TO_DATE(#{startDate}, '%Y-%m-%d %H:%i:%s') and STR_TO_DATE(#{endDate}, '%Y-%m-%d %H:%i:%s')
		order by DATE
		limit #{page}, #{size}
	</select>
		
	<select id="findEngdataForExcel" parameterType="kr.gaion.armoredVehicle.web.analysis.model.ExcelDownByMonitorDiagnos" resultType="kr.gaion.armoredVehicle.web.analysis.model.Engdata">
		select s.SDANM, e.*
		from ENGDATA e, SDA s
		where e.SDAID = s.SDAID
		and e.SDAID = #{sdaid} 
		and DATE between STR_TO_DATE(#{startDate}, '%Y-%m-%d %H:%i:%s') and STR_TO_DATE(#{endDate}, '%Y-%m-%d %H:%i:%s')
		order by DATE
	</select>

	<select id="countEnglife" parameterType="kr.gaion.armoredVehicle.web.analysis.model.troubleDataRequest" resultType="Integer">
		select ifnull(count(idx),0)
		from ENGLIFEDATA
		where AI_Trip is not null
		<if test="sdaid != null">
			and SDAID = #{sdaid}
		</if>
		<if test="startDate != null and endDate != null">
			and DATE between STR_TO_DATE(#{startDate}, '%Y-%m-%d %H:%i:%s') and STR_TO_DATE(#{endDate}, '%Y-%m-%d %H:%i:%s')
		</if>
	</select>

	<select id="findEnglife" parameterType="kr.gaion.armoredVehicle.web.analysis.model.troubleDataRequest"
			resultType="kr.gaion.armoredVehicle.web.analysis.model.Englife">
		select
			case
				when T.DISTANCE-T.AI_Trip <![CDATA[<]]> 0 then 0
				ELSE
					T.DISTANCE-T.AI_Trip
			end as REMAINDISTANCE,
			case
				when ((T.DISTANCE-T.AI_Trip) * 100 / T.DISTANCE) <![CDATA[<]]> 0 then 0
				else
					(T.DISTANCE-T.AI_Trip) * 100 / T.DISTANCE
			end as REMAINRATIO,
			T.*,
			YEARS,
			case
				when ((T.DISTANCE-T.AI_Trip) * 100 / T.DISTANCE) <![CDATA[<]]> 0 then 0
				else
					(T.DISTANCE-T.AI_Trip) * YEARS / T.DISTANCE
			end as REMAINTIME
		from (
			select
				IDX
				, AI_Trip
				, (SELECT DISTANCE FROM LIFETHRESHOLD WHERE SNSRTYPE='ENGINE') AS DISTANCE
				, (SELECT YEARS FROM LIFETHRESHOLD WHERE SNSRTYPE='ENGINE') AS YEARS
				, AI_Trip_ALGO
				, AI_Trip_MODEL
				, DATE_FORMAT(AI_Trip_DATE,'%Y-%m-%d') AS AI_Trip_DATE
				, E_OverallRMS
				, E_1_2X
				, E_1X
				, E_CrestFactor
				, FILENM
				, SDAID
				, DATE_FORMAT(DATE,'%Y-%m-%d') AS DATE
			from ENGLIFEDATA
			where 1 = 1
			and AI_Trip is not null
			<if test="sdaid != null">
				and SDAID = #{sdaid}
			</if>
			<if test="startDate != null and endDate != null">
				and DATE between STR_TO_DATE(#{startDate}, '%Y-%m-%d %H:%i:%s') and STR_TO_DATE(#{endDate}, '%Y-%m-%d %H:%i:%s')
			</if>
			order by idx
			limit #{page}, #{size}
		) T
	</select>

	<select id="findPart" parameterType="java.util.Map"
		resultType="Integer">
		select ifnull(count(*),0)
		from ENGDATA
		where AI_ENGINE = 1
		and DATE_FORMAT(`DATE`,'%Y-%m-%d') = DATE_FORMAT(#{startdate},'%Y-%m-%d')
		and SDAID = #{sdaid}
	</select>
</mapper>