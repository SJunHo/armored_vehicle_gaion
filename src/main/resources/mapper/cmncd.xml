<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.gaion.scas.monitoring.mapper.CmncdMapper">
	<select id="findListByCode" parameterType="String" resultType="kr.co.gaion.scas.monitoring.model.Cmncd">
		select *
		from cmncd
		where code like CONCAT(#{code},'%')
	</select>
		
</mapper>