<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.gaion.scas.analysis.mapper.BkdsdaMapper">

	<select id="countAll" resultType="Integer">
		select count(distinct sdaid)
		from BKDSDA
		where dttime like CONCAT('%',#{dttime},'%')
	</select>
	
	<select id="countBkdsdaBySdaid" parameterType="String" resultType="Integer">
		select count(sdaid)
		from BKDSDA
		where sdaid = #{sdaid}
		and dttime like CONCAT('%',#{dttime},'%')
	</select>
	
</mapper>